const ROOT = "/";

const FULLSCREEN = "/fullscreen";
const LOGIN = "/login";

export const whiteList = [FULLSCREEN, LOGIN];

const ROUTES = {
  ROOT,
  FULLSCREEN,
  LOGIN,
};

export default ROUTES;
